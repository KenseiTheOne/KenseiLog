namespace KenseiLog {
    /// <summary>
    /// Severity of a log record, mirroring the three UnityEngine.Debug levels.
    /// </summary>
    public enum LogLevel : byte {
        Log = 0,
        Warning = 1,
        Error = 2
    }
}
